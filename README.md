# Bluebird

Foundation for the Bluebird sandwich shop website.

## Stack

- React 19
- TypeScript
- Vite
- ESLint

## Getting Started

```bash
npm install
npm run dev
```

Open the local Vite URL in your browser to view the site.

## Available Scripts

- `npm run dev` starts the development server
- `npm run build` creates a production build
- `npm run preview` previews the production build locally
- `npm run lint` runs ESLint

## Current Structure

- `src/App.tsx` contains the one-page site sections and starter content
- `src/App.css` contains the component and section styling
- `src/index.css` contains global tokens, typography, and base styles

## Good Next Steps

- Replace placeholder copy with Bluebird brand messaging
- Add real food and interior photography
- Connect menu data, ordering links, and contact details
- Break sections into reusable components once the content stabilizes
